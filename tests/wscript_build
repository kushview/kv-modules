
import os, element

tests = bld.path.ant_glob ("test-*.cpp")

bigtest = bld.program (
    features = "cxx",
    source   = ["runner.cpp"],
    includes = ["..", "../libs", "../element"],
    name     = "runner",
    target   = "runner",
    use      = "element-0 element-engines-0 element-ui-0",
    install_path = None
)

if element.is_mac():
    bigtest.use = "ELEMENT ELEMENT_ENGINES ELEMENT_UI"
